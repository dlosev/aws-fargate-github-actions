name: PR delete

on:
  pull_request:
    types: [ closed ]

jobs:
  delete:
    if: (github.event.action == 'labeled' && contains(github.event.pull_request.labels.*.name, 'deploy'))
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo PR #${{ github.event.number }} has been merged
