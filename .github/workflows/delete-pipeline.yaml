name: PR delete

on:
  pull_request:
    types: [ closed ]

jobs:
  delete:
    if: (contains(join(github.event.pull_request.labels), 'eu-'))

    runs-on: ubuntu-latest

    steps:
      - run: |
          echo PR #${{ github.event.number }} has been merged
